# All optional — each defaults to the addresses already deployed on
# Starknet Sepolia (see lib/contracts.ts and the root README's
# "Deployment" section). Only set these if you've redeployed the
# contracts yourself and want the frontend to point at your addresses.

NEXT_PUBLIC_COLLATERAL_TOKEN_ADDRESS=
NEXT_PUBLIC_RATE_ORACLE_ADDRESS=
NEXT_PUBLIC_SWAP_CORE_ADDRESS=
