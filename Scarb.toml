[package]
name = "silhouette"
version = "0.1.0"
edition = "2024_07"
description = "Silhouette — a private interest rate swap for Starknet BTC-staking yield, built on STRK20."

[dependencies]
starknet = ">=2.8.0"

[[target.starknet-contract]]
sierra = true
casm = false

[dev-dependencies]
snforge_std = ">=0.31.0"

[tool.snforge]
